<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <title>Title</title>
    <link rel="stylesheet" type="text/css" href="style.css">
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css" integrity="sha384-BVYiiSIFeK1dGmJRAkycuHAHRg32OmUcww7on3RYdg4Va+PmSTsz/K68vbdEjh4u" crossorigin="anonymous">
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.3.1/jquery.min.js">
    </script>
    <script>
      $(document).ready(function(){

      })
    </script>
  </head>
  <body>
    <% if(messages.form_validation) { %>
        <% for (var x of messages.form_validation) { %>
        <h3><%= x %></h3>
        <% } %>
    <% } %>
  <h1> Dojo Secrets </h1>
  <p> <a href=/logout>Logout</a> </p>
  <p> Create a secret </p>
    <form action='/secrets' method='POST'>
        Secret<input type=text name=content>
        <input type=submit name=submit>
    </form>
    <p> All the secrets </p>
    <% for(var i = 0; i < secrets.length; i++) { %>
        <p><%= secrets[i].content %> <a href='/secrets/<%=secrets[i]._id%>'>Show</a></p>
        <% if(user_id == secrets[i].author_id){ %>
        <form action='/secrets/<%=secrets[i]._id%>/delete' method='POST'>
          <input type='submit' value='delete'>
        </form>
        <% } %>
    <% } %>
  </body>
</html>